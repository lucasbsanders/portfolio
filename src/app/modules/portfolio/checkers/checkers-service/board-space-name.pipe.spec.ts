import { BoardSpaceNamePipe } from './board-space-name.pipe';

describe('BoardSpaceNamePipe', () => {
  it('create an instance', () => {
    const pipe = new BoardSpaceNamePipe();
    expect(pipe).toBeTruthy();
  });
});
